Import { GatewayDispatchEvents } from 'discord-api-types/v10'; import { ClientQuest } from './src/client'; const token = process.env.TOKEN?.trim(); if (!token) { console.error('TOKEN não encontrada nas variables.'); process.exit(1); } const client = new ClientQuest(token); let isChecking = false; let initialized = false; async function checkQuests() { if (isChecking) return; isChecking = true; try { console.log('[QUEST] Fetching quests...'); await client.fetchQuests(false); const quests = client .questManager! .filterQuestsValidToDo(); console.log(`[QUEST] ${quests.length} valid quests found.`); for (const quest of quests) { try { console.log(`[QUEST] Processing: ${quest.id}`); await client.questManager!.doingQuest(quest); console.log(`[QUEST] Completed: ${quest.id}`); await new Promise((r) => setTimeout(r, 5000)); } catch (err) { console.error(`[QUEST ERROR] ${quest.id}`, err); } } } catch (err) { console.error('[FETCH ERROR]', err); } finally { isChecking = false; } } client.once(GatewayDispatchEvents.Ready, async ({ data }) => { console.log(`Logged in as ${data.user.username}`); if (initialized) return; initialized = true; await checkQuests(); setInterval(async () => { await checkQuests(); }, 1000 * 60 * 5); }); process.on('unhandledRejection', (reason) => { console.error('[UnhandledRejection]', reason); }); process.on('uncaughtException', (error) => { console.error('[UncaughtException]', error); }); client.connect(); 
